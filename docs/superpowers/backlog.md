# ChronoLoop Frontend Rewrite — Backlog

Running list of parked/flagged items from the `CHRONOLOOP-frontend` rewrite. Not tracked via git history because this is a multi-phase project — check here first before assuming something was missed.

Each item: what, where it was noticed, why it was parked instead of fixed inline.

## From Phase 1 review

- [ ] **Avatar fallback gradient** — `TopBar.tsx`'s team-avatar cluster and user-avatar button (`src/components/layout/TopBar.tsx`) render a bare `<img>` with no `onError` handling. If an avatar file 404s or fails to load, the user sees a broken-image icon instead of an initials-on-gradient placeholder. Parked because a real fallback belongs with the `Avatar` design-system primitive (Phase 2), not bolted onto TopBar directly.

- [ ] **Test-ordering footgun in `App.test.tsx`** — `useThemeStore` (`src/store/themeStore.ts`) is a module-level Zustand singleton with no reset between tests. `themeStore.test.tsx` resets it explicitly in `beforeEach`, but `App.test.tsx` does not. Any future test in that file that clicks a theme toggle (via `Sidebar`) will leak `theme` state into later tests in the same run, order-dependent and hard to debug. No visible failure yet — flagged so the next person adding an App-level test doesn't get bitten.

- [ ] **README polish** — `CHRONOLOOP-frontend/README.md` covers structure/setup/extension points adequately but wasn't given a final editing pass (tone, redundant phrasing, missing "Testing" conventions section). Cosmetic, deferred rather than blocking Phase 1 sign-off.

- [ ] **Focus-ring / parity conflict** — `global.css`'s `:focus-visible` rule (`outline: 2px solid var(--accent-blue); outline-offset: 2px; border-radius: 4px;`) hardcodes a 4px radius, but several ported components (nav items, buttons) use their own radius (e.g. Sidebar `.navItem` at 8px). The focus ring's corner radius doesn't match the element it outlines on those components. Original `index.html` may have had the same mismatch (true parity) or may not have — not yet verified against the source. Needs a decision in Phase 2 when Button/other primitives formalize focus styling.

- [ ] **Avatar payload size** — `public/avatars/*.png` are 600KB–1MB each (5 files, ~4MB total) for images displayed at 28–36px. Never compressed/resized after being copied from the old repo. Deferred because it's asset pipeline work, not component work, and doesn't block Phase 1/2 functionality.

## From Phase 1 scaffold plan

- [ ] **"Integration" label inconsistency** — the sidebar nav label for the integrations page reads "Integration" (singular), matching `index.html`'s original text verbatim (see `Sidebar.tsx` `NAV_ITEMS`, and the original at `index.html:3001-3003`), while the route (`/integrations`), the page component (`IntegrationsPage`), and the rest of the design doc use "Integrations" (plural). Preserved as literal parity rather than silently "fixed." Needs a product-owner call: keep the original typo, or correct it now that it's been surfaced.

- [ ] **Theme not persisting on reload** — `themeStore.ts`'s `useThemeStore` has no `localStorage`/`persist` middleware, matching `index.html`'s original `setTheme()` (which also doesn't persist — always resets to `dark` on full page load). Intentional parity, not a bug, but flagged because it's the kind of thing a product owner may want changed now that it's a proper SPA. If persistence is wanted, it's a deliberate behavior change beyond parity — call it out explicitly before adding it.

## From Phase 3.1 (Dashboard)

- [ ] **Team grid email vs. member-detail email mismatch** — `src/data/mockDashboardTeam.ts`'s `gridEmail` (`'Joedoe@gmail.com'`, shown under every name in the Team Status grid) and `detailEmail` (each person's real address, shown in the Member Detail modal) come from two different hardcoded sources in the original (`index.html:3247,3252,3257,3262` vs. `index.html:6639-6644`). Preserved as literal parity rather than silently unified. Needs a product-owner call: keep both, or make the grid show the real per-person email now that it's centralized in one data file.

- [ ] **Three-dot project-row menu is always visible, not hover-revealed** — `CriticalProjectsPanel.module.css`'s `.threeDotBtn` omits the original's `opacity: 0` / `.row:hover { opacity: 1 }` treatment (`index.html:544,548`) so keyboard and touch users can reach it. Deliberate accessibility improvement over strict parity, not an oversight — flagged in case a visual diff review calls it out as a regression.

- [ ] **Decorative SVG triangle omitted from KPI delta rows** — `StatCard.tsx`'s delta row renders only the unicode arrow (▲/▼); the original also renders a small SVG triangle icon next to it (`index.html:3105`), visually redundant with the arrow. Explicitly reviewed and declined during Phase 3.1 (human ruling: skip) rather than silently dropped — noted here for completeness, not expected to be revisited unless a visual diff review disagrees.

- [ ] **`MemberDetailModal`'s card-width override uses `!important`** — `MemberDetailModal.module.css`'s `.card { max-width: 360px !important; }` fights `Modal.module.css`'s own `.card { max-width: 480px }` rather than composing cleanly. The established pattern for this exact kind of one-off primitive override (see the `Avatar` `style` prop added in Phase 3.1 Task 1) is an inline `style` passthrough rather than a CSS-Modules className fight — `Modal` doesn't currently accept a `style` prop. Fix: add `style?: CSSProperties` to `Modal`'s props (mirroring `Avatar`'s extension) and switch `MemberDetailModal` to use it, removing the `!important`.

- [x] **"Select Project" dropdown option casing/sourcing is inconsistent** — `TeamStatusPanel.tsx`'s Select Project dropdown derives its first three options from `DASHBOARD_CRITICAL_PROJECTS` (whose title is `'Web 3 app for Fxtrade'`, lowercase "app" — correct for the Critical Projects row it's sourced from) while the original's dropdown item reads `Web 3 App for Fxtrade` (`index.html:5476`, capital "App"). Separately, the 4th option ("ChronoLoop Launch") is bolted on as a one-off item outside the `.map()` rather than living in the same list. A dedicated `PROJECT_OPTIONS` constant (distinct from `DASHBOARD_CRITICAL_PROJECTS`, which should stay scoped to the Critical Projects panel) would fix both the casing and the asymmetry. This is the same underlying gap as the "full project list has no shared data source yet" issue noted during Task 6 — likely worth resolving once the Projects page (Phase 3.2) establishes a real shared project list. **Resolved in Phase 3.4** — see the Phase 3.4 backlog section below.

- [ ] **`TeamStatusPanel` tracks `project` and `projectLabel` as two parallel states** — they're only ever written together (in `selectProject`), so a single `selectedProject: string | null` would express the same thing with less surface area for the two to drift. Cosmetic; not a bug today.

- [ ] **`DashboardHeader`'s CSV export leaks a timer and a blob URL** — the 1600ms `setTimeout` in `handleExport` isn't cleared on unmount (it still fires and triggers a download after navigating away), and `URL.createObjectURL` is never paired with `revokeObjectURL`. No security concern today since the CSV is a hardcoded string literal with no user input — but flagging now because once this gets wired to real task data (user-entered titles, project names), CSV-injection escaping (`=`, `+`, `-`, `@`-prefixed cells) will need to be added at the same time.

- [ ] **Filter dropdown's checkboxes may not be keyboard-reachable inside a Radix `DropdownMenu`** — `DashboardHeader.tsx`'s Filter panel puts raw `<input type="checkbox">` elements and Clear/Apply buttons inside `Dropdown.Content` (Radix `DropdownMenu.Content`), which manages roving focus across `MenuItem`s and closes on Tab by default. Never interactively/browser-verified (Phase 3.1 had no browser available in its execution environment) — if this holds, the filter panel is effectively mouse-only. If confirmed, `DropdownMenu.CheckboxItem` or swapping the whole panel to a `Popover`-based primitive would be the correct fix.

- [ ] **`.weekBtn`/`.roleBtn` CSS rule block is duplicated across 3 files** — near-identical rules appear in `CriticalProjectsPanel.module.css`, `CalendarWidget.module.css`, and twice within `TeamStatusPanel.module.css` (role dropdown + select-project dropdown). The original had one shared `.dropdown-btn` class. Consider a shared CSS module or a small `ui/` primitive once a third or fourth page needs the same "small dropdown trigger chip" look.

- [ ] **Phase 3.1 was never interactively verified in a real browser** — every task's automated test suite passes (95/95 at Phase 3.1's close) and static wiring was checked, but no environment with a browser was available during implementation to click through KPI animations, all four modals, calendar pill positioning, or the toast-emitting dropdowns. Recommended: do one manual `pnpm dev` pass through the Dashboard page before/alongside starting Phase 3.2, to catch anything automated tests can't (visual glitches, animation timing feel, real click-through flows).

- [ ] **Global Constraints' "never modify the old repo" rule needs a carve-out for `docs/`** — Phase 3.1's plan stated "Never modify `C:\Users\HP\Downloads\Chronoloop dashboard\`" as a blanket rule, but its own Task 9 correctly required editing `docs/superpowers/backlog.md` in that same repo (this file). The intent was always "don't touch `index.html`/`design.md`/the old app," not "don't touch the planning docs that happen to live alongside it." Future phase plans should word this constraint as "never modify the old app itself (`index.html`, assets, `design.md`) — `docs/` in that repo is the shared planning-docs location and is fair game."

## From Phase 3.2 (Tasks page — list/board views)

- [ ] **Task Stats Row counts are computed live, matching the original's own behavior** — the original's `chip-total`/`chip-inprogress`/etc. (`index.html:3313-3337`) are seeded with literal `15/4/5/3/3` markup, but that markup is only the pre-render placeholder: `updateChips()` (`index.html:7099-7107`) recomputes all five counts live from `tasksData` and is called at the top of every `renderTasksPage()` (`index.html:7110`) — on nav, add, delete, checkbox-toggle, sort, filter, and search — so the hardcoded literals are overwritten before a user ever sees them. This port's `TaskStatsRow` computing counts live from `tasksStore` is therefore **strict parity** with the original, not a deliberate departure from it. (An earlier version of this entry mischaracterized the original as using "static" counts unrelated to `tasksData` — that was a misreading of the code, since `MOCK_TASKS`'s live distribution — 15/4/5/3/3 — matching the hardcoded placeholder exactly is expected, not coincidental, once `updateChips()` is accounted for.)
- [x] **Task detail (row click / kanban card click) shows a placeholder toast, not a detail view** — Phase 3.3 will replace `TasksPage`'s `handleOpenDetail` with the real task detail side panel. Until then, `TaskRow`/`TaskBoardCard` correctly call `onOpenDetail(id)`, they just don't have anywhere real to send it yet.
- [ ] **`tasksStore.ts` (Task 1) — three small cosmetic/latent items**: `setTaskStatus` casts `status as TaskStatus` unnecessarily; `STATUS_CONFIG` mixes literal hex `color` values with CSS-var `dotColor` values (the `--accent-*` vars are confirmed to exist, so this is fine for now, but worth remembering if a future consumer reads `dotColor` expecting a literal color); and `todoKpiOverride` is only recalculated by `addTask` — `updateTask`/`removeTask`/`restoreTask`/`setTaskStatus` don't touch it, so it can go stale relative to the real `tasks` array once Dashboard's KPI tiles are cross-checked against Tasks-page actions (this staleness is verified parity with the original, not an unintended gap: `index.html:6705-6706` only updates the KPI inside the create branch of its submit handler, and the edit branch, `deleteTaskWithUndo`, and the checkbox handler all leave it untouched too).
- [ ] **`AddTaskModal.reset.test.tsx` (Task 2) has a stale doc-comment** — it references a `sessionRef` name left over from an abandoned earlier implementation attempt. Comment-only, no functional effect.
- [ ] **`useDeleteWithUndo`/`toastStore` (Task 3) — two latent smells**: the hook's `setInterval` has no unmount-cleanup (low risk — it self-terminates within 5 ticks and Zustand setters are safe to call post-unmount, but worth a comment or unmount-aware option if a future caller's closures assume the component stays mounted); and `showActionToast` hardcodes `variant: 'error'` for all action toasts, which is fine for today's only caller (delete) but a latent smell if reused for a non-error case later.
- [ ] **`getInitials` (Task 4) 2-character/no-whitespace special case** would also match a genuine 2-letter person name with no space (e.g. "Al") — inherent to the fix design, not a defect; no current caller hits it.
- [ ] **`TasksToolbar`'s Filter panel (Task 6) doesn't close on Clear/Apply**, unlike the shipped Dashboard Filter panel's precedent (which does close on Clear/Apply). Cosmetic, since the checkboxes are non-functional either way.
- [ ] **`TaskGroup` (Task 7) — two accessibility gaps inherited from the brief's sample code**: its header nests a `role="button"` span inside a real `<button>` (an ARIA/HTML nesting anti-pattern), worked around with `aria-label` for test/accessible-name purposes but structurally unresolved; and the header has no `aria-expanded` reflecting collapsed state, only a `data-collapsed` attribute used for CSS, so screen readers get no semantic disclosure-widget signal.
- [ ] **`TaskBoardView`/`TaskBoardCard` (Task 8) — two test-coverage gaps**: no test explicitly asserts the *absence* of a priority icon on board cards (a deliberate design point, currently only implicitly true by omission); and `TaskBoardView.test.tsx` only exercises the first column's "+" button, not all four independently (low risk, shared `onClick` implementation).
- [ ] **`TaskStatsRow`'s "To Do" chip label and `TaskGroup`'s "To Do" list-view section header render identical text** (both source `STATUS_CONFIG.todo.label`) — discovered during Task 9's own TDD loop when `TasksPage.test.tsx`'s default-render test hit a "multiple elements found" error on `getByText('To Do')`. Fixed in the test (switched to `getAllByText`), not a product bug — the two "To Do" labels are visually well-separated (stat chip vs. group header) — but flagged so future test authors targeting either instance by exact text know to scope their query (`within(...)`, role, or `getAllByText`).

## From Phase 3.3 (Task Detail side panel)

- [ ] **Escape-to-close is a deliberate accessibility addition, not strict parity** — the original's global `Escape` handler (`index.html:6330-6339`) never closes `task-detail-panel`; only clicking the overlay or the explicit close button does in the shipped app. Added here per the design doc's non-negotiable keyboard-navigation standard (§4a), the same category of deviation as `CriticalProjectsPanel`'s always-visible three-dot menu (Phase 3.1).
- [ ] **The description field's empty-state placeholder is real, savable content** — when a task has no description, the `contenteditable` div's initial text is the literal string "Click to add a description...", and blur unconditionally saves whatever text is present. A user who clicks in and out without typing will overwrite an empty description with that literal placeholder string. This is the original's own behavior (`index.html:7263`, `:7358-7361`), preserved as-is, not fixed into a true non-submitting placeholder.
- [ ] **Clicking a different task row while the panel is already open takes two clicks** — the panel's full-screen overlay blocks/closes on the first click rather than also opening the newly-clicked row in the same click; a second click on the row is needed to open it. Matches the original's own overlay-blocking behavior exactly (see this plan's Global Constraints) — flagged here in case a future visual/UX review wants a "swap directly to the new task" affordance, which would be a deliberate improvement over parity, not a bug fix.
- [ ] **Comment/subtask avatars and the assignee-code→name map are duplicated a third time here**, separately from `AddTaskModal`'s own copies (Phase 3.2) — matches the original's own inline duplication at `openTaskDetail` (`index.html:7237`), not a missed shared-constant opportunity. Same category as the already-flagged 4-project-list duplication (Phase 3.2 backlog).
- [ ] **`TaskAssigneeBubble`'s `size` prop is now a bare `number`** instead of the earlier `26 | 22` literal union, which slightly weakens its compile-time guarantee that only two specific sizes are ever passed — acceptable since the component is small, internal to `src/components/tasks/`, and the original itself uses more than two size/font-size pairings across its call sites.
- [ ] **Phase 3.3 was never interactively verified in a real browser during implementation** — same standing caveat as Phases 3.1 and 3.2 (no browser available in the execution environment). Task 6 Step 6 above lists the manual `pnpm dev` walkthrough that should happen before/alongside starting Phase 3.4.
- [ ] **Attachments icon substitution: `TaskDetailAttachments`'s `fig`-type icon uses lucide-react's `Layers` icon instead of `Figma`** — the installed `lucide-react@1.31.0` npm package does not export a `Figma` icon (confirmed: `node -e "require('lucide-react').Figma"` returns `undefined`) — the original `index.html:7294` uses `data-lucide="figma"` from the CDN-hosted vanilla `lucide` icon font, which does carry that brand icon; `lucide-react`'s npm package has historically dropped some brand/trademarked icons that the CDN distribution still ships. Cosmetic only (one icon glyph for `.fig` attachments), not a behavior deviation.
- [ ] **`TaskAssigneeBubble`'s `size` prop no longer restricted to two card-and-board values** — widened from the literal union `26 | 22` to a bare `number` to support this phase's new call sites (22/28px in the detail panel and its comment avatars), which slightly weakens the compile-time guarantee that only two specific sizes are ever passed — acceptable since the component is small, internal to `src/components/tasks/`, and the original itself uses more than two size/font-size pairings across its call sites.
- [ ] **Comment input text persists across tasks** — the comment textarea's text state lives at the panel level, not per-task, so a half-typed comment on one task survives closing the panel and re-appears (and can still be sent) when a different task is opened. Matches the original's own behavior (a single static comment input, cleared only on send), not a regression — flagged as a foot-gun for future UX review.
- [ ] **Full focus-trap/return-focus-to-opener was scoped out of the Phase 3.3 fix wave** — the panel now blurs the active element and prevents tab-reachability while closed (`inert` + blur-before-close, addressing the description-edit data-loss case), but does not trap focus while open or restore focus to the row/card that opened it. Worth a small follow-up if a future accessibility pass targets this panel specifically.

## From Phase 3.4 (Projects page)

- [x] **"Select Project dropdown option casing/sourcing is inconsistent" (Phase 3.1 backlog item) — resolved.** `TeamStatusPanel` now sources its Select Project dropdown from a local `PROJECT_OPTIONS` literal (`'Web 3 App for Fxtrade'`, capitalized, matching `index.html:5476`) instead of mapping over `DASHBOARD_CRITICAL_PROJECTS` plus a bolted-on 4th item. `DASHBOARD_CRITICAL_PROJECTS` itself is untouched — it's a genuinely separate, smaller, dashboard-only hardcoded list in the original, not something this phase unified away.
- [ ] **Projects stat-chip counts are computed live, a deliberate deviation from the original's static HTML.** Unlike Tasks' `updateChips()` (confirmed genuinely live in the Phase 3.2 backlog), no function in `index.html` ever recomputes `#proj-stat-chips`' counts — they're static literals (`10/4/2/2/1/1`) that never update as `PROJECTS` changes. `ProjectStatsRow` computes them live from `projectsStore` instead, because leaving them static would desync immediately after using this same phase's own New Project/Delete actions. Flagged here in case a strict-parity visual diff calls it out.
- [ ] **Projects Sort dropdown's "Status" option is a preserved no-op**, matching `renderProjectsPage()`'s sort switch (`index.html:7547-7552`), which has no `'status'` case. Selecting it changes the active dropdown item but doesn't change list order, in both the original and this port.
- [ ] **New Project modal's "Lead Assignee" select is decorative** — its value is never read by `addProject`, matching the original's own `btn-create-project` handler (`index.html:8485-8516`), which never reads `proj-assignee-sel` either. Every new project gets the same hardcoded `team:[{i:'JA', c:'#4A90FF', n:'Jacobs A.'}]` regardless of the selected assignee.
- [ ] **New Project's `dueDays` is always hardcoded to `30`**, regardless of the chosen due date, matching `index.html:8498` exactly — not recomputed from the picked date.
- [ ] **Projects list-view column headers look interactive (cursor:pointer, hover color, a chevrons-up-down icon) but have no click handler**, matching the original — no click-to-sort wired to any column.
- [ ] **Phase 3.4 was never interactively verified in a real browser during implementation** — same standing caveat as Phases 3.1/3.2/3.3 (no browser available in the execution environment). Recommended: one manual `pnpm dev` pass through both Projects views, the detail panel, and the New Project modal before/alongside starting Phase 3.5 (Sprints).

## From Phase 3.5 (Sprints page)

- [ ] **Filter dropdown doesn't auto-close on Apply/Clear**, on both `SprintsPageHeader` and its Phase 3.4 sibling `ProjectsToolbar.tsx`. The original calls `closeAllDD()` after both actions (`index.html:8395`,`:8402`); this port's `handleApply`/`handleClear` are plain buttons inside Radix `Dropdown.Content` (not `Dropdown.Item`), so `DismissableLayer` doesn't dismiss on the inside click. More noticeable on Sprints than Projects since Apply visibly re-filters the list behind a still-open panel. Fix: a `DropdownMenu.Root` `open`/`onOpenChange` controlled pair — apply to both `SprintsPageHeader` and `ProjectsToolbar` together so fixing one doesn't introduce a new cross-page inconsistency.
- [ ] **`SprintsToolbar`'s search input CSS mirrors `ProjectsToolbar.module.css`'s established values rather than `index.html`'s own global `.search-input` rule** — a genuine self-contradiction in the Phase 3.5 plan ("same structure as ProjectsToolbar" vs. the Global Constraint "CSS ports verbatim from index.html"), inherited unaddressed from Phase 3.4 (`ProjectsToolbar.module.css` itself already diverges: `border-radius:8px`/`padding:0 10px 0 30px`/`font-size:12px` vs. the original's `24px`/`0 14px 0 34px`/`13px`, `index.html:232-259`). Worth fixing on both pages in the same pass, not Sprints alone.
- [ ] **`SprintKpiGrid`'s reuse of `StatCard` adds interactivity/animation the original's sprint KPI cards don't have** — `tabIndex={0}`, hover state, staggered `animationDelay`, and `useCountUp`, vs. the original's plain non-interactive `<div class="stat-card">` with static `textContent` (`index.html:8009-8031`, no `tabindex` — contrast Dashboard's own KPI cards, which do have `tabindex="0"` at `index.html:3098-3146`). Direct, accepted consequence of the plan's "do not fork a copy of `StatCard`" mandate — a decision, not an oversight; noted in case a strict-parity visual/interaction review flags it.
- [ ] **`sprintsStore.addSprint` prepends new sprints (`[new, ...prev]`) instead of the original's `push` (append)** (`index.html:8464`). Invisible today since the results list is always re-sorted and a fresh `s_<timestamp>` id sorts first under the default `number`-sort comparator either way — but the store's insertion order no longer mirrors the source's, worth remembering if a future consumer ever reads store order directly instead of through the sorted/filtered view.
- [ ] **`key={m.i}` on team-avatar lists (banner, list rows, board cards, detail panel) would collide if two team members shared the same 2-letter initials** — safe today against all 5 items in `MOCK_SPRINTS`, not exercised by any test. Low-priority robustness note; `${m.i}-${idx}` or a real per-member id would be sturdier if the mock roster grows.
- [ ] **`SprintKpiGrid.test.tsx` calls `vi.useRealTimers()` inline in the test body rather than in an `afterEach`**, so fake timers would leak into sibling tests if an assertion above it threw first. Matches the same pre-existing fragile pattern in Dashboard's own `KpiGrid.test.tsx` — consistent with precedent, not a regression, but both would benefit from the same `afterEach` fix together.
- [ ] **Phase 3.5 was never interactively verified in a real browser during implementation** — same standing caveat as every prior phase (no browser available in the execution environment); the final whole-branch reviewer explicitly could not perform the plan's required manual `pnpm dev` walkthrough for the same reason. Recommended before/alongside starting Phase 3.6 (Calendar): walk through List↔Board toggle, chip filter, dropdown Status filter Apply/Clear, sort, search, opening/editing/deleting a sprint from both the list-row menu and the detail panel, marking a sprint complete, and creating a new sprint — confirm the ring and burndown visibly replay on each filter/search/sort interaction, and specifically check the just-fixed avatar ring/overlap on list rows and board cards renders correctly (code-verified only, never seen).

## From Phase 3.6 (Calendar page)

- [ ] **Legend styling is a deliberate departure from the original's values** — the original's `.cal-legend` uses `font-size: 11px`, 8px dots, no opacity reduction, making it visually near-identical to the inactive `.cal-filter-chip` buttons beside it (also 11px, similar colored dots). This was flagged during visual review as confusing — the legend looked like a duplicate set of filter controls. Deliberate fix: **removed the legend entirely** (explicit reversal from the earlier "differentiate it" approach — removal is the cleaner permanent fix since the filter chips already have colored dots that communicate the same color→type mapping). The filter chips serve as both filter controls and legend.

- [ ] **"New Event" button was using a non-existent `btn-primary` global class** — `CalendarPageHeader` and `CalAgendaView` both rendered `<button className="btn-primary">` which has no corresponding CSS in this project (the design system uses the `Button` component with CSS Modules). This caused the button to render with browser defaults, appearing inconsistently across views. Fixed by switching to `<Button variant="primary">` from `src/components/ui/Button.tsx`.

- [ ] **Event popup switched from Radix Popover to centered Radix Dialog** — the original uses a positioned popup anchored near the clicked event (`index.html:10912-10924`). Three attempts at replicating this positioning (Radix Popover with dynamic anchor, fixed-position with viewport coords) all failed in edge cases (sidebar overlap, scroll containers, near-edge events). Switched to a centered Dialog (modal-style) which permanently eliminates all positioning bugs at the cost of not anchoring near the trigger. This is a deliberate departure from the original's positioning behavior.

- [ ] **Long-duration events (>=240min or multi-day) rendered as compact banners** — in Week and Day views, sprints (duration: 480min) and multi-day project milestones now render as small banners above the time grid instead of proportional time blocks that covered 50%+ of the grid and buried other events. Threshold: `duration >= 240` OR `isMultiDay`. Normal meetings (up to 3 hours) still render proportionally in the time slots. This is a deliberate departure from the original's rendering (which used full time-proportional blocks for all events regardless of duration).

- [ ] **Breadcrumb and heading were unstyled** — `CalendarPageHeader` used non-existent global classes `"page-breadcrumb"` and `"page-heading"` instead of CSS Module classes. Fixed to use `styles.breadcrumb` and `styles.heading` matching the pattern established by ProjectsPageHeader, SprintsPageHeader, and TasksPageHeader.

- [ ] **Reset button in CalendarAgendaBar was unstyled** — used non-existent `className="btn-secondary"` instead of the `<Button variant="secondary">` component. Fixed.

- [ ] **`@radix-ui/react-popover` removed as unused dependency** — installed for the original popup positioning approach, no longer needed after switching to Radix Dialog.

- [ ] **Phase 3.6 was never interactively verified in a real browser during initial implementation** — same standing caveat as every prior phase (no browser available in the execution environment during the initial build pass). Multiple bugs were caught in subsequent manual walkthroughs. All fixed before merge was finalized.

### Phase 3.6 — remaining polish / follow-up review planned

- [ ] **Dialog popup loses the "anchored near trigger" feel** — the centered Dialog is functional and solves positioning bugs permanently, but it's a UX departure from the original's contextual popup (appeared right-of-trigger). If a future pass wants to restore contextual positioning, the cleanest path would be wrapping each event pill/block in its own `Popover.Root`/`Popover.Trigger` pair inside each view component directly (not a shared wrapper in the page). This was avoided initially due to the deep nesting across 4 view components but is architecturally sound.

- [ ] **Dialog overlay darkness may not match the original's popup feel** — the overlay uses `rgba(0, 0, 0, 0.45)` which is the same as the Modal primitive. The original's event popup had no overlay at all (just an outside-click listener). The overlay dims the calendar behind it, which changes the interaction feel from "quick glance at event details" to "modal interruption." Consider reducing overlay opacity to `0.2` or removing it entirely (rely on close button + Escape + overlay click only) if the feel is too heavy.

- [ ] **Banner threshold (240min) is untested against real-world data** — the 240-minute cutoff was chosen based on the current mock data (sprints at 480min, meetings at ≤90min, projects at 120min). If future mock data or real data introduces 4-hour meetings or workshops, they'll get banner treatment which may not be desired. May need a type-based rule (e.g., only `sprint` and `project` types get banner treatment) rather than a pure duration threshold.

- [ ] **CalEventPopup progress bar animation may not fire in Dialog context** — the `useEffect` with `requestAnimationFrame` that animates the progress fill from 0% to `ev.progress%` was originally designed for a popup that mounts/unmounts on open/close. Inside a Dialog that mounts when `open` becomes true, the timing should be the same, but this hasn't been visually verified (the progress bar is only visible for task/project/sprint events that have a `progress` field).

- [ ] **Week view banner section has no max-height / scroll** — if a day has many long-duration events (unlikely with current mock data but possible with user-created events), the banner section will grow unbounded and compress the time grid. Consider adding `max-height: 60px; overflow-y: auto` if this becomes a problem.

- [ ] **Filter chip "All" has no colored dot while the other 4 do** — matches the original, but visually the "All" chip looks slightly misaligned (shorter) next to the others. Cosmetic only.

- [ ] **`CalendarSubHeader.module.css` still contains `.legend`, `.legendItem`, `.legendDot` rules** — dead CSS left over from the legend removal. Should be cleaned up in a follow-up pass.

- [ ] **A dedicated follow-up visual review pass is planned for this page** — the initial implementation had no browser access, and subsequent fixes were verified structurally but not fully walked through interactively across all states (e.g., creating a user event via the modal, verifying it appears in all 4 views, clicking it to open the Dialog, using "View Task" navigation, filter interaction after adding events, Today button after navigating away). Recommend a focused 10-minute click-through before starting Phase 3.7.

## From Phase 3.7 (Team page)

- [ ] **Filter dropdown is a real functionality gap, not just a parity quirk** — the Filter button opens a dropdown with Status (Online/Offline) and Department checkboxes plus Apply/Clear buttons, but Apply/Clear only show toasts and never actually filter the list. The only real filters are the department tabs and search. This is preserved as-is from the original but flagged as a genuine UX gap: a control that looks functional but does nothing is misleading. Wire it up in a future pass (apply checkbox selections as real filters, OR remove the decorative Filter button if it's not going to be functional).

- [ ] **Activity feed sorting uses approximate time parsing** — relative time strings ("30m ago", "2h ago", "Yesterday", "2 days ago") are parsed into sort weights via `parseRelativeTime()`. This produces correct relative ordering within the mock data but would break for any activity with a truly ambiguous time string. If real timestamps are ever added to the activity data, switch to proper ISO-based sorting.

- [ ] **"Message" and "Assign Task" buttons show toasts but perform no real action** — preserved as parity. Message shows "Opening message composer…", Assign Task shows "Opening task assignment…". These are placeholder interactions awaiting real features.

- [ ] **Team page detail panel uses inline styles for some elements** (online status dot, completion progress bar, hero section layout) rather than CSS Module classes — expedient for shipping but inconsistent with the project's CSS Module convention. Consider extracting to the module in a polish pass.

- [ ] **Phase 3.7 2 test failures in full-suite run appear to be timeout flakes** — all 110 test files pass when run individually or in small batches; the 2 failures only appear in the 148-second full-suite run under heavy process load. Same pattern as prior phases. Not a code defect.

### Phase 3.7 — remaining polish

- [ ] **List view is a deliberate departure from the original** — the original's "List" toggle just sets `gridTemplateColumns: 1fr` (stacked cards). This port replaces it with a proper compact row view (avatar, name, active tasks, dept badge, completion bar, status, context menu). The original's full-card-in-a-column behavior was not a designed list view, it was a lazy toggle. Flagged as a deliberate improvement.

- [ ] **TeamMemberRow is inlined in TeamMemberGrid rather than being a separate component import** — after a browser rendering failure caused by HMR not picking up the new file, the row JSX was inlined directly in `TeamMemberGrid.tsx`. The separate `TeamMemberRow.tsx` file still exists but is unused. Clean up: either delete the file or switch back to importing it (the underlying issue was Vite module graph stale cache, not a code defect).

- [ ] **Detail panel uses inline styles for several sub-sections** (hero, completion bar, info grid content, online dot) rather than CSS Module classes. Functional but inconsistent with the project's convention. Extract to module in a polish pass.

- [ ] **"Today" and "This Sprint" buttons on Activity Feed and Leaderboard panels are decorative** — they render as plain styled buttons with no dropdown or click behavior. The original has the same: they open dropdown panels but the selections never change the displayed data. Preserved as decorative.

- [ ] **Responsive breakpoints from the original not ported** — the original has `@media (max-width: 1280px)` (3-col KPI, 1-col bottom panels) and `@media (max-width: 900px)` / `(max-width: 640px)` (2-col/1-col card grid). Not implemented in this phase since the project is desktop-only per the design doc. Would need adding if a responsive pass is ever scoped.

## From Phase 3.8 (Reports page)

- [ ] **Reports page data is a self-contained placeholder dataset disconnected from real stores** — `RPT_DATA` uses different project names ("ChronoLoop App", "CareyCare Homepage", "Eatz Landing Page") and different team members ("Jordan K.", "Sam R.", "Taylor B.") than the actual `projectsStore`/`teamStore`. None of the chart values are computed from real task/project/sprint/team data. This is a known limitation: the Reports page will need real cross-sprint/cross-store analytics computation once a backend exists to aggregate from. Until then, it's a static visual placeholder.

- [ ] **Range tabs (7D/30D/90D/12M) are decorative** — they change the active styling and show a toast but never change the displayed chart data. Logged as a real functionality gap (same category as Team page's decorative filter — a control that looks functional but does nothing).

- [ ] **Table filter dropdowns (Project Performance / Sprint Summary) are decorative** — they change the trigger label and show a toast but never filter the table rows. Same category as above.

- [ ] **Export button shows toast sequence but produces no real file** — "Preparing report export…" then "Report exported successfully!" with no actual file download. Placeholder behavior.

- [ ] **Chart tooltips use a single shared state at page level** — this means mousing quickly between charts could flash stale content briefly before the new content renders. Not a bug in practice with the current layout (charts are visually separated) but worth noting if charts are ever moved closer together.

## From Phase 3.9 (Integrations page)

- [ ] **Integrations page runs on static data with no real backend/OAuth** — connect/manage/disconnect/API key actions are UI-only mutations within the session (Zustand store, resets on refresh). No real OAuth flows, webhook delivery, or API key generation until a backend exists.

- [ ] **Range/filter dropdowns on modals and panels are decorative** — Manage modal config changes show toasts but don't persist to any backend. Same established "decorative action" pattern as prior pages.

- [ ] **Clipboard copy feature-detects `navigator.clipboard`** — if unavailable, shows a fallback toast ("Clipboard unavailable — copy the key manually"). Deliberate improvement over the original's unconditional call that would fail silently on non-secure contexts.

- [ ] **Manage modal uses inline styles for tab content** rather than CSS Module classes — expedient for shipping but inconsistent with project convention. Consider extracting to a module in a polish pass.

- [ ] **Activity panel uses `dangerouslySetInnerHTML`** for activity text containing `<strong>` tags — safe since the data is static/hardcoded mock data, but would need sanitization if ever wired to user-generated content.

- [x] **Page headings were `<div>` instead of `<h1>` — fixed cross-page** — Calendar, Team, Reports, and Integrations page headers all used `<div className={styles.heading}>` instead of `<h1>`. This was caught via `App.test.tsx`'s navigation test for Integrations (which asserts `getByRole('heading')`), then proactively found to affect 3 other already-merged pages. Fixed: all 4 pages now use `<h1 className={styles.heading}>` for their main title. The global CSS reset (`* { margin: 0 }`) prevents the `<h1>` from adding unwanted default spacing. Earlier pages (Dashboard, Tasks, Projects, Sprints) already used the correct pattern.

## From Phase 3.10 (Settings page — final page)

- [ ] **Settings is a UI shell — only theme switching is functional** — all form inputs, toggles, radio cards, selects accept user input but nothing persists or affects the app beyond the current render. The entire Settings page (Profile, Workspace, Notifications, Appearance, Security, Billing, Team & Roles) awaits a backend + persistence layer. Theme switching (Appearance tab) is the only real mutation — it wires to `themeStore` and changes the app's `data-theme` attribute.

- [ ] **Team & Roles tab sources from `TEAM_MEMBERS` store — deliberate deviation from original** — the original had a separate 8-person roster with no overlap with the Team page (different names, domains). This was determined to be an accidental mock-data artifact. Fixed: Team & Roles now shows Jacob Solayinka (Owner) + the same 8 members from `teamStore` with assigned workspace roles. This keeps the roster consistent across pages.

- [ ] **Accent color swatches are decorative** — selecting a color changes the active highlight but doesn't update any CSS variable or persist. The original has the same stub behavior. Would need a `themeStore` extension or CSS custom property mutation to be functional.

- [ ] **Password strength indicator is visual-only** — 4 bars fill based on character count (0-3/4-6/7-9/10+). No real password validation rules (min length, complexity, etc.) are enforced.

- [ ] **Settings page uses inline styles extensively** rather than CSS Module classes for form layouts, cards, and tab content. Expedient for shipping this final page but inconsistent with the project's CSS Module convention. Consider extracting to modules in a polish pass.

- [ ] **Interface Density, Font Size, and Sidebar Behavior toggles need a frontend refactor to be functional** — these are NOT waiting on a backend. They need: (1) new CSS custom properties (e.g. `--spacing-unit`, `--font-base-size`) added to `tokens.css`, (2) every component that uses hardcoded spacing/font values to reference these variables instead, (3) the Sidebar component to read a "show labels" / "animate" flag from the settings store. This is a frontend-only task, distinct from the backend-dependent placeholders (password, billing, etc.).

## Responsive Phase R.1 — polish items

- [ ] **Drawer transition timing may feel slow at 300ms** — the 300ms ease-out matches detail panel transitions elsewhere in the app, but mobile drawer interactions typically feel snappier at 200–250ms. Worth testing with real users/devices before adjusting — may be fine on actual hardware where touch feedback makes it feel faster.

- [ ] **Search overlay expand on mobile uses `onBlur` to close** — this means if the user taps a search result (once search is functional), the blur fires before the click registers, potentially closing search prematurely. Currently not an issue since search doesn't do anything (no results dropdown), but will need revisiting when search becomes functional. Consider `onMouseDown` prevention or a brief delay.

- [ ] **Drawer overlay has no transition on open** — the overlay goes from `opacity: 0` to `opacity: 1` with a CSS transition, but since `display: block` can't be transitioned and `pointer-events` swap is instant, the scrim may appear abruptly on some browsers. Consider using `visibility` + `opacity` instead of `display` + `pointer-events` for a smoother fade.

- [ ] **TopBar padding jump at breakpoint edges** — padding changes from 28px (desktop) → 20px (tablet) → 12px (mobile) in discrete jumps at exactly 1024px and 640px. A user resizing their browser across these boundaries sees a layout shift. Not a real-world issue (users don't resize; they're at a fixed device width), but worth noting.

- [ ] **Hamburger button has no aria-expanded attribute** — it should reflect `isDrawerOpen` state for screen readers: `aria-expanded={isDrawerOpen}`. Small accessibility gap.

- [x] **Sidebar drawer doesn't trap focus when open** — **FIXED in Phase R.2** via `useInert` hook. When the drawer is open, `<main>` gets the `inert` attribute, preventing keyboard/screen-reader focus from reaching content behind the overlay. Same pattern applied to all 4 detail panels (Tasks, Projects, Sprints, Team) — each page wraps its content in a `<div ref={contentRef}>` with `useInert(panelOpen)`. Future pages with overlay panels should follow this same pattern: wrap page content in a div with `useInert`, leave the panel component outside that wrapper.

- [ ] **Mobile search icon uses `onClick` on the SVG element** — this works but the tap target is only the 14px icon, not the full 44px wrapper. Should move the click handler to the `.searchWrap` container div for a proper 44px touch target on mobile.

- [ ] **ChevronDown on user avatar button renders at mobile** — the arrow indicator is unnecessary on mobile where there's no dropdown menu wired. Consider hiding it at <640px to save space.

## Responsive Phase R.2 — notes

- [x] **Detail panel focus-trap gap closed** — all 4 detail panels (Task, Project, Sprint, Team) now use `useInert` to make page content behind them non-tabbable when open. This pattern (`useInert<HTMLDivElement>(panelOpen)` on a wrapper div, panel rendered outside that wrapper) is now the established convention for any new overlay panel.

- [ ] **Pattern note for future phases:** any new page that adds a detail panel or overlay should follow the established `useInert` pattern: (1) import `useInert` from `src/hooks/useInert.ts`, (2) read the panel's open state from its store, (3) wrap the page's scrollable content in `<div ref={contentRef}>`, (4) render the panel component as a sibling outside that wrapper. This ensures keyboard focus is automatically trapped in the panel when open.

## Responsive Phase R.3 — notes

- [ ] **Reports inline styles fully extracted to CSS Module** — `ReportsPage.tsx` no longer uses inline `gridTemplateColumns` styles. The new `ReportsPage.module.css` has `.chartRow2col`, `.chartRow3col`, `.tableRow2col` classes with responsive breakpoints. This is the correct pattern for any future page that needs responsive grid layouts — use CSS Module classes, not inline styles.

- [ ] **Dashboard CalendarWidget relies on horizontal scroll at mobile** — the 19-column pill grid scrolls horizontally via `overflow-x: auto`. This works but isn't ideal UX on mobile (hidden content requires discovery). A future pass could consider a simplified 7-day or 5-day view for mobile, but that's a larger interaction change beyond R.3's scope.
